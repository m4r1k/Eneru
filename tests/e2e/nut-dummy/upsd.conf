# upsd configuration for E2E testing
# Listen on all interfaces so container is accessible

LISTEN 0.0.0.0 3493

# Max age of data before it's considered stale (seconds)
MAXAGE 15

# Allow connections from any host (for container networking)
# In production, you'd restrict this
