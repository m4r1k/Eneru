# ISS-043/048: single source of truth. This file used to hand-list dev deps
# and drifted from pyproject's `dev` extra (playwright missing here, mkdocs
# listed here but living in the `docs` extra). Install the extras directly so
# the two can never disagree again. Docs tooling lives in docs/requirements.txt
# (consumed by Read the Docs) and the `docs` extra.
-e .[dev,notifications,auth,mqtt]
